export  const handleDeleteRow = (rows, selectionRow) => {
  return new Promise ((resolve, reject) => {
    // const id = row_params.id;
    // console.log("row_params");
    // console.log(row_params);
    // console.log("id");
    // console.log(id);
    // row_params.api.updateRows([{ id, _action: "delete" }]);
    console.log("Row DELETED!");
    // set_row_params({});
  })

  };